@model List<ProductSection>

@{
    Layout = "AdminLayout";
}
<div class="row d-flex justify-content-between mt-4 pl-4 pr-4">
    <h4 class="text-secondary">Administrator Area for Product Sections</h4>
    <a class="btn btn-light" asp-action="CreateProductSection" asp-controller="Admin">
        <i class="fas fa-plus"></i>
        <span class="ml-2">Create</span>
    </a>
</div>
<div class="row p-2">
    @if (Model.Count == 0)
    {
        <h5 class="pl-3">There is no Product Section in Database</h5>
    }
    @foreach (var productSection in Model)
    {
        <div class="col-lg-3 col-md-4 col-sm-6 card-group">
            <div class="card m-1">
                <div class="card-header">
                    <span>@productSection.Title</span>
                </div>
                <div class="card-body">
                    <div class="d-flex h-240">
                        <span>@productSection.Overview</span>
                    </div>
                </div>
                <div class="card-footer">
                    <ul class="list-group list-group-horizontal">
                        <li class="list-group-item p-0 mr-2">
                            <a asp-action="ViewProductSection" asp-controller="Admin" asp-route-id="@productSection.Id"
                               class="btn btn-primary text-white" data-toggle="tooltip" data-placement="auto" title="Show">
                                <i class="fas fa-eye"></i>
                            </a>
                        </li>
                        <li class="list-group-item p-0 mr-2">
                            <a asp-action="EditProductSection" asp-controller="Admin" asp-route-id="@productSection.Id"
                               class="btn btn-info text-white" data-toggle="tooltip" data-placement="auto" title="Edit">
                                <i class="fas fa-edit"></i>
                            </a>
                        </li>
                        <li class="list-group-item p-0 mr-2">
                            <a asp-action="DeleteProductSection" asp-controller="Admin" asp-route-id="@productSection.Id"
                               class="btn btn-danger text-white" data-toggle="tooltip" data-placement="auto" title="Delete">
                                <i class="fas fa-trash"></i>
                            </a>
                        </li>

                    </ul>
                </div>
            </div>
        </div>
    }

</div>
@section Scripts {
    <script>
        $(document).ready(function () {
            $('[data-toggle="tooltip"]').tooltip();
        });
    </script>
}